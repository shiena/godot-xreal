<!-- Generated by scripts/gen_api_docs.ps1 (Windows) / gen_api_docs.sh (mac/Linux) from the
     `///` doc comments in src/ and the `##` doc comments in addons/godot_xreal/ — DO NOT EDIT. -->

# XrealShared

**Inherits:** [`Object`](https://docs.godotengine.org/en/stable/classes/class_object.html)  
**Script:** `addons/godot_xreal/features/xreal_shared.gd`  

Static helpers shared by the godot_xreal feature components (addons/godot_xreal/features/*). Never instantiated.

Some native resources are process-global singletons, so the feature scenes must coordinate:

- XrealAR polls native change queues that are CONSUMED on poll — a second XrealAR polling the same stream would steal events. get_ar() shares exactly one node across all features.
- XrealHandTracker registers the XRServer hand trackers; one instance suffices.
- The XrealHeadTracker owns the stereo eye viewports + render driver: the app owns its lifecycle (usually via addons/godot_xreal/xreal_rig.tscn), so it is only ever looked up.
- XrealSystem, by contrast, is a stateless facade over that global state — every feature creates its own instance freely (make_system).

## Methods

<a id="method-apply_rgb_camera_geometry"></a>

### static apply_rgb_camera_geometry(system: Object, cam: Camera3D) -> Vector3

Drive a Camera3D from the glasses RGB camera's real geometry so rendered holograms line up with the camera image: intrinsics -> vertical FOV (from fy, KEEP_HEIGHT), pose-from-head -> the small forward offset, returned in Godot space (this port's Unity->Godot map (x,-y,-z)). Static per device — callers apply once and cache the returned offset. Vector3.ZERO when unavailable.

<a id="method-audio_wants_app"></a>

### static audio_wants_app(state: int) -> bool


<a id="method-audio_wants_mic"></a>

### static audio_wants_mic(state: int) -> bool


<a id="method-find_camera_feature"></a>

### static find_camera_feature(tree: SceneTree) -> Node

The XrealCamera feature component in the tree, or null. O(1) group lookup — safe per frame.

<a id="method-find_camera_feed"></a>

### static find_camera_feed(tree: SceneTree) -> Object

The live XrealCameraFeed, or null while the camera is off/absent. Consumers poll this at point of use (per frame / per capture), which makes feed sharing independent of scene-tree insertion order and of when the camera toggles.

<a id="method-find_head_tracker"></a>

### static find_head_tracker(tree: SceneTree) -> Node3D

The XrealHeadTracker (head rig), or null while it doesn't exist yet. Never auto-created — the app owns the rig's lifecycle and may spawn it late, so callers re-poll per frame / per use.

<a id="method-get_ar"></a>

### static get_ar(tree: SceneTree) -> Node

Find-or-create the ONE shared XrealAR poller. An auto-created node has all four AR stream switches (planes/anchors/images/mesh) off — each feature turns its own stream on/off — and is parented under the tree root (it survives scene changes; add_child must be deferred because the root is busy while the initial scene is still entering the tree). A user-placed XrealAR is honoured instead when it joined GROUP_AR (note its stream switches default to ON).

<a id="method-get_hand_tracker"></a>

### static get_hand_tracker(tree: SceneTree) -> Node

Find-or-create the ONE shared XrealHandTracker (registers the XRServer hand trackers /user/hand_tracker/left|right). Same sharing pattern as get_ar.

<a id="method-is_app_audio_ready"></a>

### static is_app_audio_ready() -> bool

Whether app audio can be captured right now. False until consent is granted, and again if the user revokes it from the status bar.

<a id="method-is_mic_granted"></a>

### static is_mic_granted() -> bool

RECORD_AUDIO, which the SDK's native mic capture needs. True off-Android so editor runs behave.

<a id="method-is_native_runtime"></a>

### static is_native_runtime() -> bool


<a id="method-make_system"></a>

### static make_system() -> Object

A fresh XrealSystem — a stateless facade over process-global native state, so each feature may own one. Returns null off-device (keeps the features inert on desktop).

<a id="method-request_app_audio_consent"></a>

### static request_app_audio_consent()

Ask the user for screen-capture consent, which is what app ("internal") audio needs.

Android will only let an app capture playback audio through a MediaProjection, and the XREAL encoder builds its own AudioPlaybackCaptureConfiguration from the one we hand it — so without consent a recording carries the microphone only. Returns immediately; the system dialog is asynchronous, so poll [`is_app_audio_ready()`](#method-is_app_audio_ready). Calling it again while a dialog is up, or once consent is held, does nothing.

<a id="method-resolution_preset"></a>

### static resolution_preset(level: int) -> Vector3i


## Enumerations

<a id="enum-ResolutionLevel"></a>

### enum ResolutionLevel

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-LOW"></a>`LOW` | `0` |  |
| <a id="constant-MIDDLE"></a>`MIDDLE` | `1` |  |
| <a id="constant-HIGH"></a>`HIGH` | `2` |  |
| <a id="constant-CUSTOM"></a>`CUSTOM` | `3` |  |

<a id="enum-AudioState"></a>

### enum AudioState

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-NONE"></a>`NONE` | `0` |  |
| <a id="constant-APP"></a>`APP` | `1` |  |
| <a id="constant-MIC"></a>`MIC` | `2` |  |
| <a id="constant-APP_AND_MIC"></a>`APP_AND_MIC` | `3` |  |

## Constants

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-GROUP_AR"></a>`GROUP_AR` | `&"xreal_shared_ar"` |  |
| <a id="constant-GROUP_HAND_TRACKER"></a>`GROUP_HAND_TRACKER` | `&"xreal_shared_hand_tracker"` |  |
| <a id="constant-GROUP_HEAD_TRACKER"></a>`GROUP_HEAD_TRACKER` | `&"xreal_head_tracker"` | xreal_rig.tscn's root joins this group; add it to a custom rig's XrealHeadTracker too. |
| <a id="constant-GROUP_CAMERA"></a>`GROUP_CAMERA` | `&"xreal_camera_feature"` |  |
