<!-- Generated by scripts/gen_api_docs.ps1 (Windows) / gen_api_docs.sh (mac/Linux) from the
     `///` doc comments in src/ and the `##` doc comments in addons/godot_xreal/. DO NOT EDIT. -->

# XrealStreamPairing

**Inherits:** [`Node`](https://docs.godotengine.org/en/stable/classes/class_node.html)  
**Script:** `addons/godot_xreal/features/xreal_stream_pairing.gd`  

> The script declares no `class_name`, so **XrealStreamPairing** is this reference's name for it, not a type you can write in GDScript. Attach `addons/godot_xreal/features/xreal_stream_pairing.gd` to a node of your own.

Pairs the FPV stream with XREAL's "StreamingReceiver" PC app through its LAN-discovery protocol, reverse-engineered from the SDK sample (Samples~/Camera Features/FirstPersonStreammingCast) and validated against the real receiver. See docs/plans/fpv-streaming-plan.md.

Handshake (all little-endian):

1. DISCOVERY : UDP-broadcast the ASCII "FIND-SERVER" to 255.255.255.255:6001, and the receiver replies with an ASCII "&lt;ip>:&lt;tcpPort>" string.
2. CONTROL : TCP-connect to &lt;ip>:&lt;tcpPort>. Frames are [u16 length][u16 type][payload], where length includes the 4-byte header and `type` is a MsgType. Send EnterRoom (4) and wait for its ack, then send MsgSync (7) carrying [u64 msgid][{"useAudio":bool}] and wait for the matching [u64 msgid][{"success":true}]. A HeartBeat (3) every second keeps the link alive, and the receiver echoes it.
3. The caller then streams to rtp://&lt;ip>:5555 (video 5555, audio 5557). The SDK hard-codes that port; the discovered one is only the TCP control port.

## Signals

<a id="signal-camera_param"></a>

### camera_param(fov: Dictionary)

ObserverView: the receiver pushed an `UpdateCameraParam`, the observer camera FOV as `{left,right,top,bottom}` tangents, a webcam-derived off-centre frustum. See docs/plans/observer-view-notes.md.

<a id="signal-failed"></a>

### failed(reason: String)

Pairing could not complete: discovery, connect or the handshake failed or timed out.

<a id="signal-lost"></a>

### lost()

The control link dropped after pairing, so the caller should stop streaming.

<a id="signal-paired"></a>

### paired(server_ip: String)

Pairing finished; caller should stream to rtp://&lt;server_ip>:5555.

## Methods

<a id="method-is_active"></a>

### is_active() -> bool


<a id="method-start"></a>

### start(with_audio: bool, observer: bool = false)

Begin pairing. `with_audio` is announced to the receiver as `useAudio`, on FirstPersonView only. In `observer` mode the receiver sits on its ObserverView page, where it never handles useAudio and sending it drops the link, so we go active right after the EnterRoom ack and simply consume its UpdateCameraParam pushes.

<a id="method-stop"></a>

### stop()

Stop pairing and tear down the control link, sending a best-effort ExitRoom first. Safe to call at any time.

## Enumerations

<a id="enum-MsgType"></a>

### enum MsgType

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-NONE"></a>`NONE` | `0` |  |
| <a id="constant-CONNECTED"></a>`CONNECTED` | `1` |  |
| <a id="constant-DISCONNECT"></a>`DISCONNECT` | `2` |  |
| <a id="constant-HEARTBEAT"></a>`HEARTBEAT` | `3` |  |
| <a id="constant-ENTER_ROOM"></a>`ENTER_ROOM` | `4` |  |
| <a id="constant-EXIT_ROOM"></a>`EXIT_ROOM` | `5` |  |
| <a id="constant-UPDATE_CAMERA"></a>`UPDATE_CAMERA` | `6` |  |
| <a id="constant-MSG_SYNC"></a>`MSG_SYNC` | `7` |  |

<a id="enum-State"></a>

### enum State

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-IDLE"></a>`IDLE` | `0` |  |
| <a id="constant-DISCOVERING"></a>`DISCOVERING` | `1` |  |
| <a id="constant-CONNECTING"></a>`CONNECTING` | `2` |  |
| <a id="constant-ENTER_ROOM"></a>`ENTER_ROOM` | `3` |  |
| <a id="constant-NEGOTIATE"></a>`NEGOTIATE` | `4` |  |
| <a id="constant-ACTIVE"></a>`ACTIVE` | `5` |  |

## Constants

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-DISCOVERY_ADDR"></a>`DISCOVERY_ADDR` | `"255.255.255.255"` |  |
| <a id="constant-DISCOVERY_PORT"></a>`DISCOVERY_PORT` | `6001` |  |
| <a id="constant-FIND_MSG"></a>`FIND_MSG` | `"FIND-SERVER"` |  |
| <a id="constant-HEARTBEAT_S"></a>`HEARTBEAT_S` | `1.0` |  |
| <a id="constant-DISCOVERY_TIMEOUT_S"></a>`DISCOVERY_TIMEOUT_S` | `4.0` |  |
| <a id="constant-CONNECT_TIMEOUT_S"></a>`CONNECT_TIMEOUT_S` | `5.0` |  |
| <a id="constant-HANDSHAKE_TIMEOUT_S"></a>`HANDSHAKE_TIMEOUT_S` | `5.0` |  |
