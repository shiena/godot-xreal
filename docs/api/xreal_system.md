<!-- Generated by scripts/gen_api_docs.ps1 (Windows) / gen_api_docs.sh (mac/Linux) from the
     `///` doc comments in src/ and the `##` doc comments in addons/godot_xreal/ — DO NOT EDIT. -->

# XrealSystem

**Inherits:** [`RefCounted`](https://docs.godotengine.org/en/stable/classes/class_refcounted.html)  
**Registered by:** the `godot_xreal` GDExtension  

SDK information and control surface for the XREAL glasses.

A `RefCounted` façade over the native XREAL plugin: query availability / version / tracking state, switch the tracking mode, drive the AR subsystems (plane detection, spatial anchors, image tracking, depth meshing), read compositor render metrics, and run FPV streaming. Instantiate it once (e.g. from an autoload) and keep it. Its methods are safe to call before a native session exists — they return defaults / error sentinels until the session is live, so they no-op on desktop and while the glasses are still connecting.

## Methods

<a id="method-acquire_anchor"></a>

### acquire_anchor(pose: Transform3D) -> Dictionary

Create a new anchor at `pose` (a world `Transform3D`). Returns the anchor `Dictionary` (`{ id, transform, tracking_state, session_id }`) or an empty dict on failure.

<a id="method-estimate_anchor_quality"></a>

### estimate_anchor_quality(id: String, pose: Transform3D) -> int

Estimate an anchor's save quality (`ANCHOR_QUALITY_*`) at `pose`, or `-1` on failure.

<a id="method-get_app_frame_latency_ms"></a>

### get_app_frame_latency_ms() -> float

App frame latency (motion-to-photon input) in milliseconds. `-1.0` while unavailable.

<a id="method-get_camera_intrinsics"></a>

### get_camera_intrinsics(component: int) -> PackedFloat32Array

A `COMPONENT_*` camera's intrinsics as `[fx, fy, cx, cy]` in pixels (empty when unavailable).

<a id="method-get_camera_projection_matrix"></a>

### get_camera_projection_matrix(component: int, near: float, far: float) -> PackedFloat32Array

A `COMPONENT_*` camera's 4x4 projection matrix (16 floats, Unity column-major) for `[near, far]`. Empty when unavailable.

<a id="method-get_device_pose_from_head"></a>

### get_device_pose_from_head(component: int) -> PackedFloat32Array

A `COMPONENT_*` device's extrinsic relative to Head as a raw Unity `Pose`: `[pos x,y,z, quat x,y,z,w]` (Unity left-handed — convert to Godot; see coordinate-systems-notes). Empty when unavailable.

<a id="method-get_device_resolution"></a>

### get_device_resolution(component: int) -> Vector2i

A `COMPONENT_*` device's pixel resolution (`Vector2i.ZERO` when unavailable).

<a id="method-get_device_type"></a>

### get_device_type() -> int

Connected `XREALDeviceType` enum value (`0` = invalid/unavailable).

<a id="method-get_diagnostics"></a>

### get_diagnostics() -> String

One-line diagnostic of the perception pipeline (session/clock/pose state).

<a id="method-get_dropped_frame_count"></a>

### get_dropped_frame_count() -> int

Frames dropped by the compositor. `-1` while the metrics handle is not up yet.

<a id="method-get_early_frame_count"></a>

### get_early_frame_count() -> int

Frames presented early. `-1` while the metrics handle is not up yet.

<a id="method-get_extended_frame_count"></a>

### get_extended_frame_count() -> int

Extended (re-projected/stale) frame count (EFC). `-1` while the metrics handle is not up yet.

<a id="method-get_frame_composite_time_ms"></a>

### get_frame_composite_time_ms() -> float

Compositor frame composite time in milliseconds. `-1.0` while the metrics handle is not up yet.

<a id="method-get_frame_present_count"></a>

### get_frame_present_count() -> int

Present count for the current frame (FPC). `-1` while the metrics handle is not up yet.

<a id="method-get_glasses_temperature_level"></a>

### get_glasses_temperature_level() -> int

Latest glasses temperature level from the hardware event funnel: `0` NORMAL / `1` WARM / `2` HOT (mirrors the SDK's `XREALTemperatureLevel`), or `-1` until the glasses first report one. A cached poll — no signal. This is the data source behind the SDK's over-temperature notification. (The SDK's low-battery notification reads the Android *host* battery, not a glasses API — poll it from the platform; SLAM-state is `get_tracking_state` / `get_tracking_reason`.)

<a id="method-get_hmd_time_nanos"></a>

### get_hmd_time_nanos() -> int

Current HMD clock in nanoseconds (`0` while the perception pipe is down).

<a id="method-get_last_native_error_code"></a>

### get_last_native_error_code() -> int

Latest asynchronous native error reported by the plugin, as the `XREALErrorCode` enum value (`0` Success / `1` Failure / … `-1` until one arrives). A cached poll — no signal — mirroring the SDK's native-error notification. Pair with `get_last_native_error_message()` for the accompanying text.

<a id="method-get_last_native_error_message"></a>

### get_last_native_error_message() -> String

Message that accompanied the latest native error (empty string if none / not provided).

<a id="method-get_nr_rendering_symbol_count"></a>

### get_nr_rendering_symbol_count() -> int

Number of direct NR rendering symbols resolved from libnr_loader.so.

<a id="method-get_plane_boundary"></a>

### get_plane_boundary(id: String) -> PackedVector2Array

Boundary polygon (plane-local points) of a detected plane, by its id string from `poll_planes`.

<a id="method-get_plane_detection_mode"></a>

### get_plane_detection_mode() -> int

Current `PlaneDetectionMode` flags, or `-1` when unavailable.

<a id="method-get_plugin_version"></a>

### get_plugin_version() -> String

Native plugin version string (`"n/a"` when unavailable).

<a id="method-get_present_fps"></a>

### get_present_fps() -> float

Present rate in frames/second (compositor, integer ~60). `-1.0` while the metrics handle is not up yet.

<a id="method-get_render_metrics_diagnostics"></a>

### get_render_metrics_diagnostics() -> String

One-line diagnostic / start status of the render-metrics handle.

<a id="method-get_teared_frame_count"></a>

### get_teared_frame_count() -> int

Teared frame count. `-1` when unavailable (also the SDK's own "not tracked" sentinel).

<a id="method-get_tracking_reason"></a>

### get_tracking_reason() -> int

XR-plugin tracking-reason enum value (`-1` when unavailable).

<a id="method-get_tracking_state"></a>

### get_tracking_state() -> int

XR-plugin tracking-state enum value (`-1` when unavailable).

<a id="method-get_tracking_type"></a>

### get_tracking_type() -> int

Current `TrackingType` enum value — see the `TRACKING_*` constants (`-1` when unavailable).

<a id="method-image_reference_count"></a>

### image_reference_count(handle: int) -> int

Number of reference images in a database, or `0` when unavailable.

<a id="method-init_image_database"></a>

### init_image_database(blob: PackedByteArray, image_guids: PackedStringArray, image_sizes: PackedVector2Array) -> int

Build a tracking database from a reference-image DB `blob` (produced by `trackableImageTools`) plus, per image, its baked `image_guids` (32-hex, aligned with `image_sizes`) and physical `image_sizes` (metres). Returns the DB handle (`0` on failure). Pass it to [`set_image_database()`](#method-set_image_database) to activate, and keep it for [`release_image_database()`](#method-release_image_database).

<a id="method-is_anchor_available"></a>

### is_anchor_available() -> bool

Whether the connected glasses support spatial anchors. Gated by `is_ar_perception_available()` (6DoF + no RGB camera = the Air 2 Ultra). `false` until the session is up.

<a id="method-is_available"></a>

### is_available() -> bool

Whether the native session is up (libraries loaded + session created). `false` on desktop/editor, or while waiting for the Android Activity / after a failed bootstrap.

<a id="method-is_camera_supported"></a>

### is_camera_supported() -> bool

Whether the connected glasses have an RGB camera (`IsHMDFeatureSupported(FEATURE_RGB_CAMERA)`). The Air 2 Ultra reports `false` — gate the camera on this so it is never opened there.

<a id="method-is_hmd_feature_supported"></a>

### is_hmd_feature_supported(feature: int) -> bool

Whether the connected glasses support a `FEATURE_*` capability (`IsHMDFeatureSupported`). `false` when unavailable. This is the device-accurate gate the SDK itself uses.

<a id="method-is_image_tracking_available"></a>

### is_image_tracking_available() -> bool

Whether the connected glasses support image tracking. Gated by `is_ar_perception_available()` (6DoF + no RGB camera = the Air 2 Ultra). `false` until the session is up.

<a id="method-is_meshing_supported"></a>

### is_meshing_supported() -> bool

Whether the connected glasses support depth meshing. Gated by `is_ar_perception_available()` (6DoF + no RGB camera = the Air 2 Ultra). `false` until the session is up.

<a id="method-is_nr_rendering_available"></a>

### is_nr_rendering_available() -> bool

Whether the direct NR rendering/compositor API was resolved from libnr_loader.so.

<a id="method-is_plane_detection_available"></a>

### is_plane_detection_available() -> bool

Whether the connected glasses support plane detection. Gated by `is_ar_perception_available()` (6DoF + no RGB camera = the Air 2 Ultra). `false` until the session is up, so query it at toggle time, not at startup.

<a id="method-is_session_started"></a>

### is_session_started() -> bool

Whether a native session is currently running.

<a id="method-is_stream_active"></a>

### is_stream_active() -> bool

Whether an FPV stream is currently active.

<a id="method-load_anchor"></a>

### load_anchor(guid: String) -> Dictionary

Restore a saved anchor by its `Guid` string. Returns the anchor `Dictionary` or an empty dict.

<a id="method-poll_anchors"></a>

### poll_anchors() -> Dictionary

Poll anchor changes since the last call: `{ "added": Array, "updated": Array, "removed": Array }` (each added/updated entry a `Dictionary { id, transform, tracking_state, session_id }`; `removed` an array of id strings). Call once per frame to drive the SDK's change queue.

<a id="method-poll_controller"></a>

### poll_controller() -> PackedFloat32Array

One-frame read of the live controller's raw sensors (call each frame after `start_controller`). Returns a flat `PackedFloat32Array`, layout: `[ok, accel.xyz(1..4), gyro.xyz(4..7), mag.xyz(7..10), touch(10), touch_xy(11..13), buttons(13)]`. The phone IMU (`accel` = gravity dir via `-accel.normalized()`) feeds the GDScript pointer fusion, since the NRController fused pose isn't available on this host.

<a id="method-poll_images"></a>

### poll_images() -> Dictionary

Poll tracked-image changes since the last call: `{ "added": Array, "updated": Array,  
 "removed": Array }` (each added/updated entry a `Dictionary { id, source_image, transform,  
 size, tracking_state }`; `removed` an array of id strings). Call once per frame.

<a id="method-poll_mesh_blocks"></a>

### poll_mesh_blocks() -> Array

Poll the current mesh blocks. Returns an `Array` of `Dictionary { id: String, state: int,  
 vertices: PackedVector3Array, normals: PackedVector3Array, indices: PackedInt32Array }`. `state == 2` means the block was removed; build/update an `ArrayMesh` per block otherwise.

<a id="method-poll_planes"></a>

### poll_planes() -> Dictionary

Poll detected-plane changes since the last call. Returns `{ "added": Array, "updated": Array, "removed": Array }` where each added/updated entry is a `Dictionary { id: String, transform: Transform3D, center: Vector2, size: Vector2, alignment: int }` and `removed` is an array of id strings. Call once per frame (drives the SDK's change queue).

<a id="method-release_image_database"></a>

### release_image_database(handle: int)

Free a database from [`init_image_database()`](#method-init_image_database).

<a id="method-remap_anchor"></a>

### remap_anchor(id: String) -> bool

Re-localize an anchor into the current map. Returns the SDK bool (or `false` when unavailable).

<a id="method-remove_anchor"></a>

### remove_anchor(id: String) -> bool

Drop a tracked anchor by its id. Returns the SDK bool (or `false` when unavailable).

<a id="method-save_anchor"></a>

### save_anchor(id: String) -> String

Persist an anchor (by its id from `poll_anchors`) and return its `Guid` key as a 32-hex string (empty on failure). Estimate quality ≥ SUFFICIENT first; enumerate saved keys yourself.

<a id="method-set_anchor_enabled"></a>

### set_anchor_enabled(enabled: bool) -> bool

Enable/disable the anchor subsystem (call once before acquiring). Returns whether the export was present. Needs a live 6DoF session for anchors to actually track.

<a id="method-set_anchor_mapping_dir"></a>

### set_anchor_mapping_dir(dir: String) -> bool

Point the anchor subsystem at a writable directory for its saved-anchor map files (e.g. `OS.get_user_data_dir()`). Call before saving/loading.

<a id="method-set_display_bypass_psensor"></a>

### set_display_bypass_psensor(bypass: bool) -> int

Keep the glasses display on by bypassing the proximity (wear) sensor auto-off. Returns the SDK status (0 = success), or `-1` when unavailable. The SDK no-ops until the session is live, so retry after `is_session_started()` turns true.

<a id="method-set_glasses_space_mode"></a>

### set_glasses_space_mode(mode: int) -> int

Set the glasses spatial display mode (`SetGlassesSpaceMode`, One Pro X1 chip). `mode` is the `NRGlassesSpaceMode` enum (RE / unverified — probe 0/1/2/… on device to find follow vs world-anchor). Returns the SDK status, or `-1` when unavailable. Call after `is_session_started()` is true (the SDK no-ops until NativeGlasses is ready).

<a id="method-set_image_database"></a>

### set_image_database(handle: int)

Activate a database from [`init_image_database()`](#method-init_image_database) (pass `0` to disable image tracking).

<a id="method-set_input_source"></a>

### set_input_source(source: int)

Which input sources `InitUserDefinedSettings` asks the SDK for: `1` = Controller (default), `2` = Hands, `3` = ControllerAndHands. Must be called **before** the XR rig starts the session — it is read once at bootstrap. Also settable with `adb shell setprop debug.xreal.input_source <n>`.

**Only ask for Hands if you actually use hand tracking.** The Hands bit makes the SDK call `NativePerception::SetHandTrackingEnabled(true)` synchronously during input start, measured at **~878 ms of cold start** on an X4000 + One Pro — and hand tracking is Air 2 Ultra only, so on any other headset that is pure latency. `addons/godot_xreal/features/xreal_hands.tscn` sets this to `3` from its `_ready()`, so scenes that include the hands feature get it automatically and everyone else starts faster. See `docs/archive/codex-input-start-analysis.md`.

<a id="method-set_meshing_enabled"></a>

### set_meshing_enabled(on: bool) -> bool

Enable/disable depth meshing (`NativePerception::SetMeshingEnabled`). Returns whether the call reached the SDK (perception up); poll `poll_mesh_blocks` after enabling.

<a id="method-set_plane_detection_mode"></a>

### set_plane_detection_mode(mode: int) -> bool

Enable plane detection (`PLANE_HORIZONTAL | PLANE_VERTICAL` flags). Needs a live 6DoF session; returns the SDK bool (false when unavailable). Call after `is_session_started()`.

<a id="method-set_stereo_mode"></a>

### set_stereo_mode(mode: int)

Select the stereo rendering mode applied when the native session **bootstraps** (a startup selector): `0` = Multipass (both eyes — the default shipping path), `2` = Multiview (single-pass-instanced). **Call before the session starts** (e.g. an autoload `_ready`, before the XR rig enters the tree) — it is read once at `InitUserDefinedSettings`. Equivalent to the ProjectSetting `xreal/stereo_mode` or `adb shell setprop debug.xreal.stereo_mode <n>`. Multiview buys nothing on this two-SubViewport rig (see docs/archive/multiview-investigation.md), so Multipass stays the recommended default.

<a id="method-set_tracking_type"></a>

### set_tracking_type(mode: int)

Select the head-tracking mode applied when the native session **bootstraps** (a startup selector): `0` = 6DoF (SLAM position + orientation, no drift — the recommended mode), `1` = 3DoF (IMU orientation only, no position), `2` = 0DoF. **Call before the session starts** (e.g. an autoload `_ready`, before the XR rig enters the tree) — it is read once at `InitUserDefinedSettings`. Equivalent to the ProjectSetting `xreal/tracking_type` or `adb shell setprop debug.xreal.tracking_type <n>`. Use `get_tracking_type()` for the mode actually active on the running session, and `switch_tracking_type()` to change it at runtime (SDK call; may be unavailable mid-session).

<a id="method-smoke_test_nr_rendering_create_destroy"></a>

### smoke_test_nr_rendering_create_destroy() -> int

RE probe: create and immediately destroy an NRRendering handle.

Returns 0 on success, -1 when libnr_loader.so was not resolved, or the native NRResult status on failure.

<a id="method-smoke_test_nr_rendering_start_stop"></a>

### smoke_test_nr_rendering_start_stop() -> int

RE probe: create, start, stop, and destroy an NRRendering handle.

Returns 0 on success, -1 when libnr_loader.so was not resolved, or the native NRResult status on failure.

<a id="method-start_controller"></a>

### start_controller() -> String

Discover + create + start the NRController subsystem (`libnr_loader.so`) and keep it alive for `poll_controller`. Returns a one-line diagnostic (count / id / connected &amp; handheld type). The phone-as-3D-pointer source (docs/plans/input-plan.md Phase C).

<a id="method-stream_push_frame"></a>

### stream_push_frame(gl_texture_id: int, timestamp_ns: int) -> int

Feed one frame to the running stream: `gl_texture_id` from `RenderingServer.texture_get_native_handle` on the view's texture, `timestamp_ns` in nanoseconds. **Call inside a `RenderingServer.call_on_render_thread` callback** (the encoder reads the GL texture on the render thread's EGL context). Returns the encoder status (`0` ok, `-1` if not streaming).

<a id="method-stream_start"></a>

### stream_start(output: String, width: int, height: int, bitrate: int, fps: int, with_mic: bool, with_internal_audio: bool, with_alpha: bool) -> bool

Start streaming the FPV to `output` — an `rtp://ip:port` / `rtmp://…` URL or a local file path (the URL scheme picks local/RTMP/RTP). Returns whether the encoder started. Then feed the view's GL texture each frame via [`stream_push_frame()`](#method-stream_push_frame) from a `RenderingServer.call_on_render_thread`.

<a id="method-stream_stop"></a>

### stream_stop()

Stop + tear down the FPV stream.

<a id="method-switch_tracking_type"></a>

### switch_tracking_type(tracking_type: int) -> bool

Switch the tracking mode at runtime (`TRACKING_6DOF` / `TRACKING_3DOF` / `TRACKING_0DOF` / `TRACKING_0DOF_STAB`). Returns the SDK's bool result; `false` also when the session is not up yet.

## Constants

| Constant | Value | Description |
| --- | --- | --- |
| <a id="constant-TRACKING_6DOF"></a>`TRACKING_6DOF` | `0` | `TrackingType` for `switch_tracking_type` / `get_tracking_type` (from the Unity `XREALPlugin.cs` enum): 6DoF — SLAM position + orientation (the recommended mode). |
| <a id="constant-TRACKING_3DOF"></a>`TRACKING_3DOF` | `1` | `TrackingType`: 3DoF — IMU orientation only, no position. |
| <a id="constant-TRACKING_0DOF"></a>`TRACKING_0DOF` | `2` | `TrackingType`: 0DoF — no head tracking. |
| <a id="constant-TRACKING_0DOF_STAB"></a>`TRACKING_0DOF_STAB` | `3` | `TrackingType`: 0DoF with stabilization. |
| <a id="constant-FEATURE_RGB_CAMERA"></a>`FEATURE_RGB_CAMERA` | `1` | `XREALSupportedFeature` for [`is_hmd_feature_supported()`](#method-is_hmd_feature_supported): the RGB camera. |
| <a id="constant-FEATURE_WEARING_STATUS"></a>`FEATURE_WEARING_STATUS` | `2` | `XREALSupportedFeature`: the proximity (wear) sensor. |
| <a id="constant-FEATURE_CONTROLLER"></a>`FEATURE_CONTROLLER` | `3` | `XREALSupportedFeature`: a handheld controller. |
| <a id="constant-FEATURE_HEAD_TRACKING_ROTATION"></a>`FEATURE_HEAD_TRACKING_ROTATION` | `4` | `XREALSupportedFeature`: head-tracking rotation (3DoF). |
| <a id="constant-FEATURE_HEAD_TRACKING_POSITION"></a>`FEATURE_HEAD_TRACKING_POSITION` | `5` | `XREALSupportedFeature`: head-tracking position (6DoF). |
| <a id="constant-COMPONENT_RGB_CAMERA"></a>`COMPONENT_RGB_CAMERA` | `crate::ffi::component::RGB_CAMERA` | `XREALComponent` id for the geometry getters: the RGB camera. |
| <a id="constant-COMPONENT_DISPLAY_LEFT"></a>`COMPONENT_DISPLAY_LEFT` | `crate::ffi::component::DISPLAY_LEFT` | `XREALComponent` id: the left display. |
| <a id="constant-COMPONENT_DISPLAY_RIGHT"></a>`COMPONENT_DISPLAY_RIGHT` | `crate::ffi::component::DISPLAY_RIGHT` | `XREALComponent` id: the right display. |
| <a id="constant-COMPONENT_GRAYSCALE_LEFT"></a>`COMPONENT_GRAYSCALE_LEFT` | `crate::ffi::component::GRAYSCALE_CAMERA_LEFT` | `XREALComponent` id: the left SLAM grayscale camera. |
| <a id="constant-COMPONENT_GRAYSCALE_RIGHT"></a>`COMPONENT_GRAYSCALE_RIGHT` | `crate::ffi::component::GRAYSCALE_CAMERA_RIGHT` | `XREALComponent` id: the right SLAM grayscale camera. |
| <a id="constant-PLANE_NONE"></a>`PLANE_NONE` | `0` | `PlaneDetectionMode` flag for [`set_plane_detection_mode()`](#method-set_plane_detection_mode) / [`poll_planes()`](#method-poll_planes): detection off. |
| <a id="constant-PLANE_HORIZONTAL"></a>`PLANE_HORIZONTAL` | `1` | `PlaneDetectionMode` flag: detect horizontal planes (floors, tables). |
| <a id="constant-PLANE_VERTICAL"></a>`PLANE_VERTICAL` | `2` | `PlaneDetectionMode` flag: detect vertical planes (walls). |
| <a id="constant-PLANE_BOTH"></a>`PLANE_BOTH` | `3` | `PlaneDetectionMode` flag: detect both horizontal and vertical planes. |
| <a id="constant-ANCHOR_QUALITY_INSUFFICIENT"></a>`ANCHOR_QUALITY_INSUFFICIENT` | `0` | Anchor-quality level from [`estimate_anchor_quality()`](#method-estimate_anchor_quality): insufficient — do not save here. |
| <a id="constant-ANCHOR_QUALITY_SUFFICIENT"></a>`ANCHOR_QUALITY_SUFFICIENT` | `1` | Anchor-quality level: sufficient — the minimum recommended before saving. |
| <a id="constant-ANCHOR_QUALITY_GOOD"></a>`ANCHOR_QUALITY_GOOD` | `2` | Anchor-quality level: good — a strong spot to save an anchor. |
