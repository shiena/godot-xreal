# Rust
/target/
# Cargo.lock is committed: this crate builds a final artifact (the GDExtension .so), so a
# pinned lockfile gives reproducible CI / release builds.

# Godot
.godot/
*.import
demo/addons/
# Godot Android build template (top-level only; NOT addons/godot_xreal/android/).
/android/
jniLibs

# Build artifacts (incl. the desktop dummy stubs built into dummy/ by
# scripts/build_dummy_libs.ps1 / .sh — build locally, don't commit)
*.dll
*.so
*.dylib
*.apk

# Local device-test artifacts / scratch
/log
/log-unity
/godot_xreal_external.png
/android_old/
/.cargo/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db


# XREAL addon vendored binaries (AARs) — vendored locally
# (see docs/plans/android-export-plugin-migration.md), like jniLibs/*.so.
/addons/godot_xreal/android/*.aar

# XREAL NR perception manifest — SDK-derived and may change across SDK versions, so it is vendored
# locally by scripts/vendor_xreal_libs.* (like the .aar), not committed. Staged into the APK's
# assets/ by export_plugin.gd; loads libnr_image_tracking.so for image tracking.
/addons/godot_xreal/android/nr_plugins.json

# XREAL host build tools (trackableImageTools — the image-tracking DB blob generator), vendored
# locally by scripts/vendor_xreal_libs.*; large SDK binaries, not committed.
/addons/godot_xreal/tools/

# XREAL addon built libraries (Android GDExtension .so + desktop dummy stubs), placed by
# scripts/build.* and scripts/build_dummy_libs.* — build artifacts, not committed.
/addons/godot_xreal/bin/

# Image-tracking reference data — SDK-derived / large binaries, kept local only.
# The reference.json manifest, the built .bin databases, and the source images
# (and their Godot .import metadata) are provided per-project, not committed.
/demo/image_tracking/reference.json
/demo/image_tracking/*.bin
/demo/image_tracking/*.png
/demo/image_tracking/*.jpg
/demo/image_tracking/*.import
